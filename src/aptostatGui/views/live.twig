<div class="front_messagebox">
    {% if not realTime %}
        Could not connect to live
    {% else %}
        <ol class="tree">
            {% for serviceGroupName, serviceGroup in realTime %}
                <li>
                    {% if (serviceGroup['groupState'] == 'up') %}
                        <label for='{{ serviceGroupName }}'>
                            <strong>{{ serviceGroupName }}</strong>
                            <div class="pull-right"><img src='../img/check.png' alt='OK'/> <strong>OK</strong></div>
                        </label>
                        <input type='checkbox' id='{{ serviceGroupName }}' />
                    {% elseif (serviceGroup['groupState']) == 'warning' %}
                        <label for='{{ serviceGroupName }}'>
                            <strong>{{ serviceGroupName }}</strong>
                            <div class="pull-right"><img src='../img/warning.png' alt='WARNING'/> <strong>Problem</strong></div>
                        </label>
                        <input type='checkbox' checked id='{{ serviceGroupName }}' />
                    {% else %}
                        <label for='{{ serviceGroupName }}'>
                            <strong>{{ serviceGroupName }}</strong>
                            <div class="pull-right"><img src='../img/cross.png' alt='ERROR'/> <strong>Down</strong></div>
                        </label>
                        <input type='checkbox' checked id='{{ serviceGroupName }}' />
                    {% endif %}

                    <ol>
                        {% for service, state in serviceGroup if service != 'groupState' %}
                            <li class="file">
                                {{ service }}
                                {% if state == 'up' %}
                                    <div class="pull-right"><img src='../img/check.png' alt='OK'/> OK</div>
                                {% elseif state == 'warning' %}
                                    <div class="pull-right"><img src='../img/warning.png' alt='ERROR'/> Problem</div>
                                {% else %}
                                    <div class="pull-right"><img src='../img/cross.png' alt='ERROR'/> Down</div>
                                {% endif %}
                            </li>
                        {% endfor %}
                    </ol>
                </li>
            {% endfor %}
        </ol>
    {% endif %}
</div>